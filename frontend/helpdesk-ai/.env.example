# Copy this file to .env and adjust the values for your machine.
# Only variables prefixed with NG_APP_ are exposed to the browser bundle by @ngx-env/builder.

# Base URL of the HelpDesk AI backend API.
NG_APP_API_BASE_URL=http://localhost:5006/api
